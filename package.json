{
  "name": "jsbyte",
  "version": "0.0.0",
  "private": true,
  "scripts": {
	  "build": "yarn wsrun -t -e -m build",
	  "lint": "yarn wsrun -e -m lint",
	  "checkup": "yarn build && yarn lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
