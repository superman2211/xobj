{
  "name": "jeng",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
	  "build": "yarn wsrun -t -e -m build",
	  "lint": "yarn wsrun -e -m lint",
	  "checkup": "yarn build && yarn lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
